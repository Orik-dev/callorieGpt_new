# app/bot/handlers/help.py
from aiogram import Router
from aiogram.filters import Command
from aiogram.types import Message, InlineKeyboardMarkup, InlineKeyboardButton

router = Router()

@router.message(Command("help"))
async def handle_help(message: Message):
    """Показывает помощь"""
    text = """🆘 <b>Помощь</b>

<b>Как пользоваться:</b>
Просто пиши или говори как удобно — я пойму.

📸 Отправь фото еды
📝 Напиши что съел
🎤 Надиктуй голосовым

<b>Примеры (но можно по-другому):</b>
- "съел борщ и хлеб"
- "на завтрак овсянка с бананом"
- "кофе с молоком"

<b>Если хочешь только узнать калории:</b>
- "сколько в пицце калорий?"
- "а если съесть бургер?"

<b>Передумал или ошибся:</b>
- "убери последнее"
- "я не ел это"
- "отмени"

<b>Редактировать:</b>
- "там было меньше"
- "на самом деле 150 грамм"

<b>Команды:</b>
/food — история за 7 дней
/profile — профиль и статистика
/subscribe — больше запросов

Я понимаю естественную речь — не нужно заучивать команды."""

    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(
            text="💬 Написать в поддержку",
            url="https://t.me/guard_gpt"
        )]
    ])

    await message.answer(text, reply_markup=keyboard, parse_mode="HTML")